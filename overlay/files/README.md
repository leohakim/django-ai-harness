# Reserved: static overlay files

Place files here if future overlay versions need to copy verbatim assets into generated projects.
Prefer `write_file_if_missing` / marked settings blocks in `apply.py` for most changes.
