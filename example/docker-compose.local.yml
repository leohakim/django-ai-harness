volumes:
  harness_example_local_postgres_data: {}
  harness_example_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: harness_example_local_django
    container_name: harness_example_local_django
    depends_on:
      - postgres
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: harness_example_production_postgres
    container_name: harness_example_local_postgres
    volumes:
      - harness_example_local_postgres_data:/var/lib/postgresql/data
      - harness_example_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres
