# Golden example

This tree is generated by `scripts/refresh-example.sh` (cookiecutter-django + overlay).
Do not hand-edit except when fixing overlay output; prefer changing the overlay and regenerating.

## Secrets / env files

Generated with `keep_local_envs_in_vcs=n`, so `.envs/` is **not** committed.
Create local env files from cookiecutter-django docs / Compose samples before running Docker.
Never commit real production credentials.
