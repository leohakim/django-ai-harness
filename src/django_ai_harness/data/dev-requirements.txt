# DX dependencies the overlay adds to [dependency-groups].dev of generated projects.
#
# Pinned with `==` on purpose:
#   1. It matches cookiecutter-django's own pinning style.
#   2. It makes `scripts/refresh-example.sh` deterministic, so the golden `example/`
#      only changes when a human (or the biweekly bot) intends it to.
#
# Bumped by .github/workflows/dx-dependencies.yml, which opens a PR every two weeks.
# Never replace `==` with `>=` here: that reintroduces non-reproducible regeneration.

django-browser-reload==1.21.0
django-linear-migrations==2.19.0
django-read-only==1.21.0
django-rich==2.2.0
django-version-checks==1.16.0
ipython==9.16.1
