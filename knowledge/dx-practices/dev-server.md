# Development server & shell

- Use Django Debug Toolbar locally (cookiecutter default).
- Enable **django-browser-reload** for template/static refresh.
- Prefer **IPython** for `shell_plus` / django-extensions workflows.
- Use **django-read-only** when exploring production-like data dumps to avoid accidental writes.
- Consider Rich / django-rich for clearer management command output.
