# Python development mode

Set for local debugging sessions:

```bash
export PYTHONDEVMODE=1
uv run python manage.py runserver
```

Benefits include extra runtime warnings for potential issues. Not a substitute for tests or Ruff.
